[build]
  # Netlify builds the Vite app and publishes static output.
  command = "npm run build"
  publish = "dist"

[[redirects]]
  # React Router needs every route to return index.html.
  from = "/*"
  to = "/index.html"
  status = 200
